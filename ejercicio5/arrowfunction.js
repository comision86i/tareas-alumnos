/*function suma(a, b) {
    let resultado = a + b;
    return resultado;
    }
    document.write(`el resultado de la suma es ${suma(3, 7)}`); */


let suma = (a, b) => {
    let resultado = a + b;
    return resultado;
}

document.write(`el resultado de la suma es ${suma(3, 7)}`);